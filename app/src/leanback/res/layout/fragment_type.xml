<?xml version="1.0" encoding="utf-8"?>
<!--
  左右留白由外层 activity_vod.xml 的主体统一提供（设计稿：主体内留白 48px @2x = 24dp），
  本文件不再重复加 24dp，否则会变成 48dp 双重留白。
-->
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:orientation="vertical">

    <!-- 本分类首位影片的推荐位：与下方卡片同一左边距，无有效影片时整体收起 -->
    <FrameLayout
        android:id="@+id/bannerWrap"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/kiwi_content_gap"
        android:visibility="gone">

        <include
            android:id="@+id/banner"
            layout="@layout/adapter_home_banner"
            android:layout_width="match_parent"
            android:layout_height="@dimen/kiwi_banner_height" />

    </FrameLayout>

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeLayout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <com.fongmi.android.tv.ui.custom.ProgressLayout
            android:id="@+id/progressLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <com.fongmi.android.tv.ui.custom.CustomVerticalGridView
                android:id="@+id/recycler"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipChildren="false"
                android:clipToPadding="false"
                android:paddingBottom="@dimen/kiwi_space_lg"
                app:focusOutEnd="true"
                app:focusOutFront="true" />

        </com.fongmi.android.tv.ui.custom.ProgressLayout>
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

</androidx.appcompat.widget.LinearLayoutCompat>
