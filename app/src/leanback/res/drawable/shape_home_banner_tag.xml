<?xml version="1.0" encoding="utf-8"?>
<!-- 标签胶囊：设计稿 24px @2x = 12sp 字，内留白 12px = 6dp，圆角 8px = 4dp -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@color/brand_primary" />

    <corners android:radius="@dimen/kiwi_radius_tag" />

    <padding
        android:bottom="6dp"
        android:left="10dp"
        android:right="10dp"
        android:top="6dp" />

</shape>
